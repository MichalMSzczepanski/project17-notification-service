5 issues:
  - message params can be null (activation for instance) throws NPE currently
4 logic:
  - log process
  - custom exceptions
  - sms client
  - abstraction for notification execution
  - configure event deletion from kafka once consumed
3 semantics:
  - more tests
  - move email contents to bean and inject
2 config:
  - logging configuration
  - pom.xml cleaning (user-service benchmark)
  - request head
1 git:
  - lorem
wishlist:
  - lorem
  - aspects:
      - lorem

